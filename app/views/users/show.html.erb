<h2><%= @user.fname %>'s Page</h2>
<h4>Email: <%=@user.email%></h4>

<% if @user == @current_user %>
	<br><br>
	<%= link_to "[Delete My Account]", user_path(current_user.id), method: :delete, confirm: "Are you sure you want to delete your account?" %>
<% end %>
