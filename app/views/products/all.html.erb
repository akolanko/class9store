<div class="center">
	<h2>All Products</h2>
	<% @products.each do |product| %>

		<%= link_to(image_tag("/clothing#{product.id}.jpg", :class => "thumb"), category_product_path(product.category.id, product.id)) %>

		<br>

	<% end %>
</div>